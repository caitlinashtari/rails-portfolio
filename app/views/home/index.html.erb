<h1>HOME!</h1>

<% @repos.each do |repo| %>
  <div class="card center">
    <p><%= repo["name"] %></p>
    <p><%= repo["description"] %></p>
    <p><%= repo["url"] %></p>
    <p><%= repo["language"] %></p>
  </div>
<% end %>
